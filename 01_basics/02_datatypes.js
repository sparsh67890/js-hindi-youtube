"use strict"; //treat all JS code as ewer version

// alert("") ,not use in node.js
let state;
let age = 5  
/*NOTE: If variable is not defined with let then while changing the variable in function,first globally scoped varible gonna changed 
        else if let then first locally scoped will changed */
console.log(state) // when we dont insert the vakue in variable ,then it is undefined

console.log(typeof "hitesh")
console.log(typeof age)
comsole.log(typeof null)